<%= form_for current_user.active_relationships.build,
  html: {class: "inline"}, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, user.id %></div>
  <%= f.submit t("user.follow"), class: "btn btn-primary inline" %>
<% end %>
